# Domain (or subdomain) pointing at this server's public IP via a DNS A record.
# Required so Caddy can obtain a TLS certificate for it.
DOMAIN=yourdomain.com

# Shared secret every client must present to connect (see README's auth section).
# Leave blank to disable auth — not recommended for a server reachable from the internet.
COLLAB_TOKEN=change-me
